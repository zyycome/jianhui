<?xml version="1.0" encoding="UTF-8" ?>     
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="zyycome.db1.dao.Db1Dao">
	
	<select id="load" resultType="map" parameterType="map" statementType="STATEMENT">
		select ${fields} from ${sourcetable} where ${keyfield}>${curid} order by ${keyfield} limit 10
	</select>

</mapper>